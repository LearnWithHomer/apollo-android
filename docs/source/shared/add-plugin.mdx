import {MultiCodeBlock} from 'gatsby-theme-apollo-docs';

In your app Gradle file, apply the `com.homer.apollographql.apollo` plugin:

Using the [plugins DSL](https://docs.gradle.org/current/userguide/plugins.html#sec:plugins_block):

<MultiCodeBlock>

```kotlin
plugins {
  // ...
  id("com.homer.apollographql.apollo").version("x.y.z")
}
```

```groovy
plugins {
  // ...
  id("com.homer.apollographql.apollo").version("x.y.z")
}
```

</MultiCodeBlock>

Or using the [legacy syntax](https://docs.gradle.org/current/userguide/plugins.html#sec:old_plugin_application):

<MultiCodeBlock>

```kotlin
buildscript {
  // ...
  classpath("com.homer.apollographql.apollo:apollo-gradle-plugin:x.y.z")
}

apply(plugin = "com.homer.apollographql.apollo")
```

```groovy
buildscript {
  // ...
  classpath("com.homer.apollographql.apollo:apollo-gradle-plugin:x.y.z")
}

apply plugin: "com.homer.apollographql.apollo"
```

</MultiCodeBlock>

The plugin is hosted on the Gradle plugin portal, Jcenter and Maven Central.
